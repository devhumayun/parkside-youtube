import { Search } from "lucide-react"

const SearchBar = () => {
    return (
        <div className="cursor-pointer">
            <Search size={20} />
        </div>
    )
}

export default SearchBar
